{
	"__example": {
		"description": "module description, ignored by parser",
		"origin": "repo origin, ignored by parser",
		"explicit_load": "if true, this mod won't be loaded until its name is specifically called through r3v\\Mod::loadMod(mod_name)",
		"is_composer": "if package uses composer or our custom format",
		"route_dir": "string, this will point to location where views are stored; defaults to /",
		"route_scope": "which scope this thing is in",
		"autoload": "additional autoload overrides",
			"include_path": "append these to PHP's include path"
	},
	"__example": {},

	"boris": {
		"origin": "git@github.com:joddie/boris.git completion-and-network-hacks",
		"is_composer": true,
		"explicit_load": true
	},
	"google-api": {
		"origin": "git@github.com:google/google-api-php-client.git",
		"is_composer": true,
		"autoload": {
			"include_path": "/src/"
		}
	},
	"scssphp": {
		"origin": "git@github.com:leafo/scssphp.git",
		"is_composer": true,
		"explicit_load": true,
		"autoload": {
			"files": ["/scss.inc.php"]
		}
	},
	"r3v/api": {
		"is_composer": false,
		"route_scope": "/api/"
	},
	"r3v/main": {
		"is_composer": false,
		"route_scope": "/"
	},
	"r3v/static": {
		"is_composer": false,
		"explicit_load": true,
		"route_scope": "/static/"
	}
}
